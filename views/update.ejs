<!DOCTYPE html>
<html>

<head>
    <title>Update Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="container col-lg-6">
        <h3>ToDo Application</h3>
        <!-- Form to add a task -->
        <form method="POST" action="/update/<%= todo._id %>">
            <div class="row">
                <div class="col">
                    <input type="text" class="form-control" value="<%= todo.todo %>" name="todo" required>
                </div>
                <div class="col">
                    <select id="tasktype" name="category" class="form-control">
                        <option value="Personal" <%= todo.category === 'Personal' ? 'selected': ''%> >Personal</option>
                        <option value="Professional" <%= todo.category === 'Professional' ? 'selected': ''%> >Professional</option>
                    </select>
                </div>
                <div class="col">
                    <input type="date" class="form-control" name="date" value="<%= todo.date.toISOString().split('T')[0] %>">
                </div>
                <div class="col">
                    <button type="submit" class="btn btn-primary">Update</button>
                </div>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>